{
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "start:connected": "turbo run start:connected",
    "start:production": "turbo run start:production",
    "next:start": "turbo run next:start",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "build2": "npm install && turbo run build"
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "tsconfig": "*",
    "turbo": "latest"
  },
  "name": "BdrWhitelabel-turbo",
  "packageManager": "npm@10.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
